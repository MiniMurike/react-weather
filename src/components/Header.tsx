import React from "react";

import "../styles/Header.css";

function Header() {
    return (
        <header>
            <a href="/"><span>React Weather App</span></a>
        </header>
    );
}

export default Header;